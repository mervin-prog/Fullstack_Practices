
<%- include('partials/header.ejs') %>

<div class="container">
      <div class="card">
      <div class="modal-content rounded-4 shadow">
          <div class="modal-header p-5 pb-4 border-bottom-0">
            
            <h1 class="fw-bold mb-0 fs-2">Welcome : <%= user.first_name %> <%= user.last_name %> !</h1>
            
          </div>
      
          <div class="modal-body p-5 pt-0">
            <% if (user) { %>
                  <h2>Your Profile:</h2>
                  <ul>
                        <li>First Name : <%= user.first_name%></li>
                        <li>Last Name : <%= user.last_name %></li>
                        <li>Email : <%= user.email %></li>
                  </ul>
            <% } else { %>
                  <p>No profile data found.</p>
            <% } %>


            <!-- Edit and Delete -->
            <!-- <form action="#" method="post">
      
              <div class="form-floating mb-3">
                <input type="email" class="form-control rounded-3" id="floatingInput" placeholder="" name="email">
                <label for="floatingInput">Email</label>
              </div>
              <div class="form-floating mb-3">
                <input type="password" class="form-control rounded-3" id="floatingPassword" placeholder="" name="password">
                <label for="floatingPassword">Password</label>
              </div>
             
            </form> -->


            <!-- Log out -->
            <form action="/logout" method="get">
                <button class="w-100 mb-2 btn btn-lg rounded-3 btn-dark" type="submit">Log out</button>
            </form>
          </div>
        </div>
      </div>
</div>

<%- include('partials/footer.ejs') %>